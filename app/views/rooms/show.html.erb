
<%= javascript_pack_tag 'oekaki' %>
<%= javascript_pack_tag 'picload' %>
<div id="current_draw_number" class="<%= @room.current_drawing_number %>"></div>
<div id="current_rotation" class="<%= @room.rotation %>"></div>
<div id='user_info' data-room_name="<%= @room.room_name %>" data-room_id="<%= @room.id %>" data-player_amount="<%= @players.count %>" data-current_rotation="<%= @room.rotation %>" data-user_name="<%= @user.username %>" data-user_id="<%= @user.id %>" data-randorder="<%= @user.randorder %>"></div>

<ul id="player-index">
    <% @players.each do |player| %>
        <li class=<%= player.id %>><%= player.username %></li>
    <% end %>
</ul>

    <div id="btn-reload">

    </div>
    <canvas
        id="draw-area"
        width="400px"
        height="400px"
        style="border: 1px solid #000000; "></canvas>
    <div id="ranges">
        <p>線の太さ<input type="range" min="0" max="100" value="5" id="line-width"><span id="width-num">5</span></p>
    </div>

</div>

<%= form_for @picture, :url => { :controller => :picpost, :action => :create }  do |f| %>
    <div class="field">
        <%# f.label :image %>
        <%# f.hidden_field :image, :value => '' %>
        <%# f.file_field :image %>
        <%# f.hidden_field :user_id, :value => current_user.id %>
        <%# f.hidden_field :room_name, :value => '' %>
        <%# @room.room_name %>
    </div>
    <button type="button" id="picpost-btn">保存</button>
        <%# f.submit "Tweetする" %>
<% end %>


<div id="picreload">
        <%# <h1>Ajax更新エリア</h1> =%>
</div>
